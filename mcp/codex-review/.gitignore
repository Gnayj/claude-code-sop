# Install artifacts. node_modules is reinstalled by the install wizard (design §4.9).
# dist/ is intentionally committed in the published repo so installs need no build step.
node_modules/
*.tsbuildinfo
