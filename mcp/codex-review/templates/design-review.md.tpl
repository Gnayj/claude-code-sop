# Default codex-review design-review template (bundled with the npm package).
#
# Projects may override this file by placing one at `.codex-review/templates/design-review.md.tpl`.
# The bundled default mirrors the per-repo default; modify per-repo, not here.

{{__bundled_default_marker__}}
